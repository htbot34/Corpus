"""Optional secondary sources. They merge into the same corpus, never the focus.

Adding a platform should mean one new file in this package. If it requires
editing synthesize.py, the abstraction is wrong.
"""
